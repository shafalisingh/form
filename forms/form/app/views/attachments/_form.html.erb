<%= form_with(model: attachment, local: true) do |form| %>
  <% if attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :scrapy_config_id %>
    <%= form.number_field :scrapy_config_id, id: :attachment_scrapy_config_id %>
  </div>

  <div class="field">
    <%= form.label :type_of_attachment %>
    <%= form.text_field :type_of_attachment, id: :attachment_type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
